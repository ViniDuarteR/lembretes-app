{
  "name": "lembretes-app",
  "private": true,
  "version": "1.0.0",
  "description": "Aplicativo de Lembretes de Saúde",
  "workspaces": [
    "packages/frontend",
    "packages/backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm:dev --workspace=frontend\" \"npm:dev --workspace=backend\"",
    "build": "npm run build --workspace=frontend",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "eslint": "^9.32.0",
    "prettier": "^3.3.3",
    "typescript": "^5.8.3"
  }
}